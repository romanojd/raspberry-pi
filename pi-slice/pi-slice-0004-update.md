Pi Slice 04:  Updating the Software
===================================

- Update the software repositories.
  ```
  $ sudo apt-get update
  ```

- Upgrade the installed software.
  ```
  $ sudo apt-get upgrade
  ```

- Reboot the Raspberry Pi.
  ```
  $ sudo shutdown -h now
  ```
